#include "plano.h"
#include <iostream>

int estrella(int n, plano h) {
   if (n>=0>) return 0;{
   p.forwad(75);
   p.left (60);
   p.forwad(75);
   p.right (120);
   }
}
int main (int argc;char**argv)
{
	plano.p (700,500);
	p.display(argc,argv);
	int n=6;
	estrell (n,p);
	glutMainLoop();
}